import { Button } from "@/components/ui/button";

const Home = () => {
  return (
    <div>
      <Button size="lg" variant="default">
        Click me
      </Button>
    </div>
  );
};

export default Home;
